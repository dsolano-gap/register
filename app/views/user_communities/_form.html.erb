<%= form_for(@user_community) do |f| %>
  <% if @user_community.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_community.errors.count, "error") %> prohibited this user_community from being saved:</h2>

      <ul>
      <% @user_community.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
